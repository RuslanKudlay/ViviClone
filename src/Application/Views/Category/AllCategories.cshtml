@*@model IEnumerable<Application.Models.Models.CategoryModel>

<div class="widget-sidebar widget_archive">

    <div class="widget-title">
        <h3>Categories</h3>
    </div>

    <ul>
        @foreach (var item in Model)
        {
            <li>
                <a href='@Url.Action("ArticlesSelectedByCategoryId", "Article" , new {categoryId = item.Id})'>
                    @Html.DisplayFor(modelItem => item.Name)
                </a>
                <span> @Html.DisplayFor(modelItem => item.ArticleCount) </span>
            </li>
        }
    </ul>

</div>*@
